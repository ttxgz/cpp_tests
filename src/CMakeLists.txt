#find_package(CURL
#	CONFIG	
#	REQUIRED
#	PATH /usr/lib NO_DEFAULT_PATH)
find_package(libcurl MODULE REQUIRED)
AUX_SOURCE_DIRECTORY(${CMAKE_CURRENT_SOURCE_DIR} DIR_TEST_SRCS)
ADD_LIBRARY ( TestObjects STATIC ${DIR_TEST_SRCS})
TARGET_LINK_LIBRARIES( TestObjects INTERFACE mycurl)
